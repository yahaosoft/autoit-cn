###User Defined Function###
_WinNet_UseConnection

###Description###
将本地设备连接到网络资源

###Syntax###
#Include <WinNet.au3>
_WinNet_UseConnection($hWnd,$sLocalName, $sRemoteName[, $sUserName = 0[, $sPassword = 0[, $iType = 1[,
$iOptions = 1]]]])

###Parameters###
@@ParamTable@@
$hWnd 
   网络资源提供者可用来作为对话框主窗体的窗体句柄. 当在选项参数中设置了数位2(互动)时使用此参数. 该参数可为0.
$sLocalName 
   要被重定向的本地设备名, 如"F:"或"LPT1". 字符串大小写不敏感. 如果为0, 不必重定向到本地即可连接到网络资源.
$sRemoteName 
   要连接的网络资源的名称
$sUsername 
   建立连接的用户名. 如果为0, 函数使用默认用户名.
$sPassword 
   用于建立连接的密码. 如果为0, 使用默认密码. 如果字符串为空, 没有密码.
$iType 
   指定要链接的网络资源的类型:
    0 - 任意(仅当$sLocalName为0时)
    1 - 磁盘
    2 - 打印机
$iOptions 
   连接选项. 可为一个或多个下列值:
    1 - 记住网络资源连接
    2 - 出于认证目的, 操作系统可与用户互动
    4 - 不使用任意用户名或密码默认设置的系统不要求用户提供选择的机会. 该标志被忽略除非选项2(互动)也被设置.
    8 - 建立连接时强制本地设备重定向
    16 - 操作系统建议使用命令行代替界面进行用户认证. 该标志被忽略除非选项2(互动)也被设置.
    32 - 如果该项被设置, 操作系统提供一个证书, 该证书被证书管理器保存. 如果证书管理器对于呼叫者的登录会话无效, 或网络提供者不支持保存证书, 则忽略该标志. 给标志也可被忽略除非设置了数位5(用命令行代替界面).
@@End@@

###ReturnValue###
@@ReturnTable@@
成功: 返回如下格式数组:
   $aInfo[0] - 如果为真, 使用本地设备重定向建立连接
   $aInfo[1] - 如果sLocalName指定为本地设备, 则为本地设备名. 如果sLocalName不指定设备 且网络需要本地设备重定向, 设置了选项4(强制重定向)时, 该缓冲区接收重定向的本地设备的 名称. 否则, 复制到缓冲区的名称是一个远程资源.
@@End@@

###Remarks###

###Related###

###See Also###
搜索MSDN知识库中WNetUseConnection的相关信息