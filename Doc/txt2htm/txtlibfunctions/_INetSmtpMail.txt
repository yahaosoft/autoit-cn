###User Defined Function###
_INetSmtpMail

###Description###
不使用外部邮件程序发送邮件.

###Syntax###
#Include <INet.au3>
_INetSmtpMail ( $s_SmtpServer, $s_FromName, $s_FromAddress, $s_ToAddress [,$s_Subject [,$as_Body [,$s_helo, [,$s_first [,$b_trace]]]]])

###Parameters###
@@ParamTable@@
 $s_SmtpServer  发送邮件所使用的SMTP服务器地址或者是数字IP地址. 为防止垃圾信息 , 大多数ISPs要求服务器这样设置. 如 "smtp.ispdomain.com", "mail.ispdomain.com" 或 "192.168.1.1"
 $s_FromName  你希望显示的发送人名称. 如 "Bob Smith"
 $s_FromAddress  你希望显示的发送的邮件地址. 如 "bob.smith@mydomain.com"
 $s_ToAddress  收件方地址. 如 "jane.brown@yourdomain.com"
 $s_Subject  [可选] 邮件主题.
 $as_Body  [可选] 邮件内容作为一个单一的字符串数组.数组中每个值在邮件里面会被以@CRLF结束.
 $s_helo  [可选]
      SMTP服务器链接的标志符(通常默认是@ComputerName).如果SMTP服务器需要一个 "EHLO" 字串 , 那就设置字符串为 "EHLO " & @ComputerName.
 $s_first  [可选]
      在helo之前发送给SMTP服务器的字符串(通常默认是{SPACE}).如果不发送任何对象这个参数必须设置为-1 , 某些 SMTP 服务器需要.
 $b_trace  [可选] 在一个窗口里面跟踪对话过程
@@End@@

###ReturnValue###
@@ReturnTable@@
成功: 返回1 失败: 返回0并且设置@ERROR为:
    1 - 无效参数
    2 - 无法启动TCP
    3 - 无法解析IP
    4 - 无法创建socket
    5x - 无法打开SMTP会话. x显示STMP服务器处理的最后命令的索引数.
    50x - 无法发送内容.X显示$as_Body的行号(第一行是 0).
    5000 - 无法关闭SMTP会话
@@End@@

###Remarks###
这个函数直接通过SMTP服务器发送邮件而不用第三方邮件工具.需要AutoIt3 v 3.1.1.97或者更高版本.

###Related###
 _INetMail

###See Also###

###Example###
@@IncludeExample@@