###Function###
ConsoleRead

###Description###
读取AU3进程中的 STDIN 流字符.

###Syntax###
ConsoleRead ( [peek = false[, 二进制 = false ]])


###Parameters###
@@ParamTable@@
peek
	[可选参数] 如果为true(真),当从流里面读取字符后不移除流里面的字符.
二进制
	[可选参数] 如果为true,从流里面读取数据使用二进制代替文本(默认为文本).
@@End@@

###ReturnValue###
@@ReturnTable@@
成功:	返回读取的字符.@extended 中包含了读取的字节数.
失败:	设置 @error 到非0,如果EOF 不能接受, STDIN 不能连接到进程或者其它错误.
@@End@@


###Remarks###
ConsoleRead 内容从控制台的autoit脚本标准输入流中读取,这通常被控制台程序用来读取父进程的输入.
ConsoleRead 不会屏蔽,它会立即返回.为了得到所有数据,它必须在调用于一个loop循环中.
Peeking on the stream does not remove the data from the buffer, however, it does return the available data as normal.
默认情况下,返回的数据是文本格式,如果使用二进制选项,其数据将返回二进制格式.

###Related###
ConsoleWrite, ConsoleWriteError, Run


###Example###
@@IncludeExample@@
